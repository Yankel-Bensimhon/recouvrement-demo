# PostgreSQL Connection
DB_USER=your_db_user
DB_HOST=localhost
DB_NAME=recouvrement_app
DB_PASSWORD=your_db_password
DB_PORT=5432

# Server Configuration
PORT=3001

# JWT Secret (if using JWT authentication later)
# JWT_SECRET=your_super_secret_jwt_key
